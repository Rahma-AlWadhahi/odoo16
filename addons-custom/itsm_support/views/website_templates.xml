<odoo>

    <!-- ======================================
         Support Form Page
    ======================================= -->
    <template id="support_form_template" name="Support Form">

        <t t-call="website.layout">

            <div class="container mt-5 mb-5" style="max-width: 720px;">
                <h2 class="mb-4">Submit a Support Ticket</h2>

                <form action="/support/create"
                      method="post"
                      enctype="multipart/form-data"
                      class="o_website_form">

                    <!-- CSRF -->
                    <input type="hidden"
                           name="csrf_token"
                           t-att-value="request.csrf_token()"/>

                    <!-- Name -->
                    <div class="mb-3">
                        <label class="form-label">Your Name *</label>
                        <input type="text"
                               class="form-control"
                               name="name"
                               placeholder="John Doe"
                               required="required"/>
                    </div>

                    <!-- Email -->
                    <div class="mb-3">
                        <label class="form-label">Email *</label>
                        <input type="email"
                               class="form-control"
                               name="email"
                               placeholder="john@email.com"
                               required="required"/>
                    </div>

                    <!-- Title -->
                    <div class="mb-3">
                        <label class="form-label">Ticket Title *</label>
                        <!-- must match controller + model -->
                        <input type="text"
                               class="form-control"
                               name="subject"
                               placeholder="Cannot login to portal"
                               required="required"/>
                    </div>

                    <!-- Description -->
                    <div class="mb-3">
                        <label class="form-label">Description</label>
                        <textarea class="form-control"
                                  name="description"
                                  rows="5"
                                  placeholder="Explain your issue..."></textarea>
                    </div>

                    <!-- Attachments -->
                    <div class="mb-3">
                        <label class="form-label">Attachments</label>
                        <input type="file"
                               name="attachment"
                               multiple="multiple"
                               accept="image/*"
                               class="form-control"/>
                        <small class="text-muted">
                            You can upload multiple files
                        </small>
                    </div>

                    <button type="submit" class="btn btn-primary w-100">
                        Submit Ticket
                    </button>

                </form>
            </div>

        </t>
    </template>


    <!-- ======================================
         Thank you page
    ======================================= -->
    <template id="support_thanks_template" name="Support Thanks">

        <t t-call="website.layout">

            <div class="container mt-5 text-center">

                <h2 class="mb-3">✅ Ticket Submitted</h2>

                <t t-if="ticket">
                    <h5>
                        Ticket Number:
                        <strong class="text-primary">
                            <t t-esc="ticket.name"/>
                        </strong>
                        —
                        <t t-esc="ticket.subject"/>
                    </h5>
                </t>

                <p class="mt-3">Our team will contact you soon.</p>
                
                <a href="/my/tickets" class="btn btn-primary mt-3">
                    View My Tickets
                </a>
                
                <a href="/" class="btn btn-secondary mt-3">
                    Back to Home
                </a>

            </div>

        </t>

    </template>

</odoo>
